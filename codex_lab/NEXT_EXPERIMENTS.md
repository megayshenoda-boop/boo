# Next Experiments

هذا الملف هو مسار الحركة داخل `codex_lab` حتى لا يتوه الشغل.

## المرحلة 1: تثبيت الخريطة

- توليد inventory ثابت من `commands.py`
- ربط كل method بالـ opcode ونوع الإرسال
- تمييز direct builders عن composite helpers
- توثيق أي drift بين `protocol.py` والوثائق

## المرحلة 2: تثبيت معاني الردود

- جمع الردود المتكررة من artifacts الحالية
- عمل جدول أولي لـ `0x026D`, `0x0042` والردود المتكررة الأخرى
- تحديد أي الردود تعتبر ack فقط وأيها يحتمل أن يكون نجاحا فعليا

## المرحلة 3: أولويات التحقق العملي

الأولوية ليست لكل الأوامر. الأولوية لهذه المجموعة:

1. `train`
2. `build`
3. `research`
4. `start_march`
5. `heal_troops`
6. `use_item`

## المرحلة 4: قبل أي اختبار حي

- نقل الأسرار من الكود إلى `.env`
- تحديد مسار رئيسي واحد: `game_bot.py` أو `bot.py`
- تعريف success criteria لكل أمر قبل التجربة
- منع الاعتماد على "وصل رد" كإثبات كاف

## القاعدة الذهبية

لا نضيف أوامر جديدة لمجرد أن الاسم موجود. نغلق دائرة أمر مهم بالكامل:

- builder صحيح
- response mapping واضح
- semantic proof
- state update مفهوم

ثم ننتقل لغيره.
